<!DOCTYPE html>
<html lang="fr">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <title>Modifier le lead #<%= leadId %></title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <!-- <link rel="stylesheet" href="/styles.css"> -->
    <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0-beta3/css/all.min.css" rel="stylesheet">
    <style>
        body {
            background-color: #2e2e2e; /* Fond sombre */
            color: white; /* Texte en blanc */
        }
        .container {
            background-color: #d6d6d6; /* Fond gris clair */
            padding: 20px;
            border-radius: 10px;
        }
        .form-label {
            color: #32CD32; /* Label vert */
        }
        .btn-primary {
            background-color: #32CD32; /* Bouton vert */
            border: none;
        }
        .btn-primary:hover {
            background-color: #228B22; /* Changement de couleur au survol */
        }
        .btn-secondary {
            background-color: #555; /* Bouton secondaire gris */
            border: none;
        }
        .btn-secondary:hover {
            background-color: #444; /* Changement de couleur au survol */
        }
        .btn-icon {
            font-size: 18px;
            color: white;
        }
        .btn-icon:hover {
            color: #228B22; /* Effet au survol */
        }
        .alert-danger {
            background-color: #ff4d4d; /* Fond d'alerte rouge */
        }
    </style>
</head>
<body>
    <div class="container mt-4">
        <h1 class="text-center mb-4">Modifier le lead #<%= leadId %></h1>

        <% if (error) { %>
            <div class="alert alert-danger" role="alert">
                <%= error %>
            </div>
        <% } %>

        <form action="/lead/edit/<%= leadId %>" method="POST" class="needs-validation" novalidate>
            <div class="mb-3">
                <label for="amount" class="form-label">Montant (€)</label>
                <input 
                    type="number" 
                    class="form-control" 
                    id="amount" 
                    name="amount" 
                    step="0.01" 
                    value="<%= leadAmount || '' %>"
                    required
                >
                <div class="invalid-feedback">
                    Veuillez entrer un montant valide
                </div>
            </div>

            <div class="d-flex justify-content-between">
                <a href="/lead/lead-summary" class="btn btn-secondary btn-icon">
                    <i class="fas fa-arrow-left"></i> Annuler
                </a>
                <button type="submit" class="btn btn-primary btn-icon">
                    <i class="fas fa-save"></i> Mettre à jour
                </button>
            </div>
        </form>
    </div>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
    <script>
        // Validation Bootstrap
        (function () {
            'use strict'
            var forms = document.querySelectorAll('.needs-validation')
            Array.prototype.slice.call(forms)
                .forEach(function (form) {
                    form.addEventListener('submit', function (event) {
                        if (!form.checkValidity()) {
                            event.preventDefault()
                            event.stopPropagation()
                        }
                        form.classList.add('was-validated')
                    }, false)
                })
        })()
    </script>
</body>
</html>
