<!DOCTYPE html>
<html lang="fr">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <title>Résumé des Clients</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <!-- <link rel="stylesheet" href="/styles.css"> -->
    <style>
        body {
            background-color: #2e2e2e; /* Fond sombre */
            color: white;
        }
        .table {
            background-color: #d6d6d6; /* Gris clair pour la table */
            color: black;
        }
        .table th {
            background-color: #32CD32; /* Vert pour les en-têtes */
            color: white;
        }
        .table td {
            background-color: #f8f9fa; /* Fond gris très clair pour les cellules */
        }
        .table-striped tbody tr:nth-of-type(odd) {
            background-color: #e2e2e2; /* Gris légèrement plus foncé pour les lignes impaires */
        }
        .btn-custom {
            background-color: #32CD32; /* Vert pour le bouton */
            color: white;
            border: none;
            padding: 10px 20px;
            border-radius: 5px;
            text-decoration: none;
            display: inline-block;
            margin-top: 20px;
        }
        .btn-custom:hover {
            background-color: #228B22; /* Changement de couleur au survol */
        }
    </style>
</head>
<body>
    <div class="container mt-4">
        <h1 class="text-center mb-4">Résumé des Clients</h1>
        <table class="table table-striped table-bordered">
            <thead class="table-dark">
                <tr>
                    <th>ID Client</th>
                    <th>Nom</th>
                    <th>Somme Budget</th>
                    <th>Somme Tickets</th>
                    <th>Somme Leads</th>
                </tr>
            </thead>
            <tbody>
                <% if (clientSummaries && clientSummaries.length > 0) { %>
                    <% clientSummaries.forEach(client => { %>
                        <tr>
                            <td><%= client.customerId %></td>
                            <td><%= client.name %></td>
                            <td><%= Number(client.budgetSum).toFixed(2) %></td>
                            <td><%= Number(client.ticketSum).toFixed(2) %></td>
                            <td><%= Number(client.leadSum).toFixed(2) %></td>
                        </tr>
                    <% }) %>
                <% } else { %>
                    <tr>
                        <td colspan="5" class="text-center">Aucun client trouvé</td>
                    </tr>
                <% } %>
            </tbody>
        </table>
        <a href="/dashboard/dashboard" class="btn btn-custom">Retour au Dashboard</a>
    </div>
</body>
</html>
